# /home/iguchi/work/Katorin/.env.example
# Server-side (API) environment variables
# NOTE: Replace with your local Supabase values (see `npx supabase start` output)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-local-anon-key
# Service role must NOT be exposed to frontend. Keep it secure.
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key

# Password reset redirect (production/staging)
PASSWORD_RESET_REDIRECT_URL=https://your-app-domain.com/password-reset

# JWT Secret (REQUIRED for team authentication)
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-key-here

# CORS設定: 許可するオリジン（カンマ区切り）
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000